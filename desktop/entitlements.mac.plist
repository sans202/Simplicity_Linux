<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- Entitlements required for an Electron app under the hardened runtime
       (needed for Apple notarization). These allow Chromium's JIT and the
       dynamic libraries Electron loads. -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
  <!-- Vane runs its Next.js server by re-executing Electron as Node via
       ELECTRON_RUN_AS_NODE, and runs SearXNG from a bundled Python; both need
       environment variables to survive into the child process. -->
  <key>com.apple.security.cs.allow-dyld-environment-variables</key>
  <true/>
</dict>
</plist>
